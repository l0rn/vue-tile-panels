.tile-horizontal
  display flex
  flex-direction column
  height 100%
  .th-panel
    position relative
    overflow hidden
    .th-resizer
      position absolute
      bottom 0
      width 100%
      background: gray
      height 4px
      cursor ns-resize

  .th-bottom
    flex 1 0 0